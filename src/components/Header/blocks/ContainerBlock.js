import styled from 'styled-components'

import { ContainerBlock as SharedContainerBlock } from '../../../shared'

const ContainerBlock = styled(SharedContainerBlock)({
    display: 'flex',
    padding: '10px 50px'
})

export default ContainerBlock