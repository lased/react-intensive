import styled from 'styled-components'

const FieldBlock = styled.div({
    color: '#FFF',
    marginBottom: 10,

    '& a': {
        color: '#9575cd',
        fontWeight: 'bold',
        textDecoration: 'none'
    }
})

export default FieldBlock